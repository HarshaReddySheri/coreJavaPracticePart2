package comSno8.harsha.JDK_JRE_JVM;

public class JRENotes {
	
	
/*Development Tools
 ******************
 *
 * (JDK/bin) EX  java, javac, javaw, javap
 *  +
 *  (JRE) EX .Java .Class
 *   
 * 
 * JRE
 * ****
 * 
 * When you develop an application program and delivery to the client or customer,
 * they don't require any development tool.
 * Only they require run time tool, i.e available in JRE It's like java software
 * 
 * Other lib folder we have lot's of Jar files, 
 * main inside lib folder is rt.jar file 
 * -> it has large storage EX: 62 MB
 * 
 * 
 */
	
/*
 * rt.jar file --> inside JRE, Lib folder
 * 
 * rt file
 * --> open it with zip file related software like winrar software
 * 
 * --> open java folder
 * 			-> inside that lang folder EX: boolean, Character
 * --> When we write a program in java all the class will come from rt.jar
 * 
 * 	
 */
	
/*
 * -> open (util) folder inside java folder
 * EX: AbstractCollecton, AbstractList, ArrayList, ArrayDeque, LinkedList, LinkedhashMap
 * these are collection program required classes
 *	
 */
	
/*
 * EX; if we have created hashMap & add hashMap element's and you have to retrieve it from rt.jarfile
 * when you perform
 * JRE -> RUNTIME TOOLS + LIBRARIES(JAVA INTERNAL CLASSES) + JVM
 * 
 * 
 * 
 * 
 */
}
